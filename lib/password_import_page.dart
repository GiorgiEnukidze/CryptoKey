import 'package:flutter_kryptokey_1/password_list_page.dart'; 
import 'package:flutter/material.dart';

class PasswordImportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Import Passwords'), // Titre de la page d'importation de mots de passe
      ),
      body: Center(
        child: Text('Import Passwords Page Content'),
      ),
    );
  }
}
